name: Browser smoke tests

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  playwright-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - run: echo "Controller extraction browser suite runs in Tests workflow"
